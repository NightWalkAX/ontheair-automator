{
  "name": "ontheair-automator",
  "version": "0.1.0",
  "private": true,
  "description": "Internal on-premise TV broadcast scheduler that ingests media, auto-generates weekly draft schedules, and pushes approved playlists to Softron OnTheAir Video instances over REST.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/seed.js",
    "test": "SCHEDULER_DB=\"${TMPDIR:-/tmp}/otav-test.sqlite\" bash -c 'rm -f \"$SCHEDULER_DB\"* && node --test --test-concurrency=1 --experimental-test-coverage test/*.test.mjs'",
    "test:plain": "SCHEDULER_DB=\"${TMPDIR:-/tmp}/otav-test.sqlite\" bash -c 'rm -f \"$SCHEDULER_DB\"* && node --test --test-concurrency=1 test/*.test.mjs'",
    "setup": "bash setup.sh",
    "pack": "bash pack.sh"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
